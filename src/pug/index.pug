doctype html
html(lang='en')

    head

        meta(charset='utf-8')
        meta(name='viewport', content='width=device-width, initial-scale=1, shrink-to-fit=no')
        meta(name='description', content='')
        meta(name='author', content='')

        title Hari - Resume

        link(rel='icon', type='image/x-icon', href='assets/img/favicon.ico')

        // Font Awesome icons (free version)
        script(src='https://use.fontawesome.com/releases/v5.15.3/js/all.js', crossorigin='anonymous')

        // Google fonts
        link(href='https://fonts.googleapis.com/css?family=Saira+Extra+Condensed:500,700', rel='stylesheet', type='text/css')
        link(href='https://fonts.googleapis.com/css?family=Muli:400,400i,800,800i', rel='stylesheet', type='text/css')

        // Core theme CSS (includes Bootstrap)
        link(href='css/styles.css', rel='stylesheet')

    body#page-top

        // Navigation layer -- Include
        include includes/_navigation.pug

        // Page Content
        .container-fluid.p-0

            // About layer -- Include
            include includes/_about.pug
            hr.m-0


            // Experience layer -- Include
            include includes/_experience.pug
            hr.m-0

            // Education
            include includes/_education.pug
            hr.m-0


            // Skills layer -- Include
            include includes/_skills.pug
            hr.m-0


            // Interests layer -- Include
            include includes/_projects.pug
            hr.m-0
            

            // Awards layer -- Include
            include includes/_certificates.pug

        // Bootstrap core JS
        script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js')

        // Core theme JS
        script(src='js/scripts.js')